# 交接文档（下个会话先读这份）

> 当前状态 + 下一步 + 卡住的东西。比 `CLAUDE.md` 细、比 `待办清单.md` 即时。
> 读法：这份 → `CLAUDE.md`（黄金律） → `FILE_MAP.md`（改哪） → `待办清单.md`（没做的）
>
> 更新日期：**2026-09-25** ｜ 测试基线：**528 全绿** ｜ 工作区有一批未跟踪杂物（见文末）

---

## 一、这一轮（2026-09-24~25）做了什么

### 第二轮（2026-09-25 晚，**尚未提交**）

- **corpus 召回修好了**：判据从"余弦阈值 + 关键词门"换成"**门放行的直通 + 门没放行的交 LLM 判**"。
  新增 `corpus_judge.py`（+ 常量 / `retrieve_corpus_candidates` / core 接入 / 21 个新测试），
  评测加 co6~co8 并改成**镜像线上**，`retrieval_eval` **28/29 → 31/32**。
  详见第三节（含"asks 那条路为什么不成立"的实测）。
- **新增 `注入链路.md`**：素材层 + 注入层的完整解剖（每层实测字符数、六路阈值、RRF trace、
  兜底方向、为什么这么设计），外加**调研中发现的 8 个问题**（含"base system 吃掉 61~84% 注入预算"、
  `session.json` 里 52 个回归测试脏键、13 条 corpus 被截断）。
- **未处理**：仓库根有一批未跟踪杂物（`astra.*` / `smc.*` / `cr*.json` / `h1.html` 等，约 24 个）
  + `记录.txt`（用户手放的真实翻车对话，不入库是**故意的**）。

### 第一轮（2026-09-24~25）

全部已推送（`6b4bdab` → 当前 HEAD）。按提交：

| 提交 | 做了什么 |
|---|---|
| `4dbe279` | **素材本土化** + 行为去措辞规定 + 删掉"近况"注入 |
| `d0e551e` | **QQ 引用消息**（用 `event.reply`——适配器早就取回了，我们从来没用） |
| `ef3f5f5` | **判据回归集** + 修好 `retrieval_eval.py`（它导入即崩，坏了很久没人知道） |
| `a81c282` | **周表拖拽更新**（`更新周表.bat`）+ 文档同步 |
| `126765d` | **群聊接话门**（群里不再"有话就接"） |
| `354f935` | **读合并转发**（摘要后注入）+ **发表情包**（24 张 + 离线打标） |
| `f6ab27c` | 表情包防重复：提示词不可靠，代码再拦一道 |
| `8a45411` | **主动发言**（动态/微博 → 她会主动说的那句话） |
| `f7b27ef` | **检索层体检**：量出噪声地板，修掉一路 + 查明两路判据失效 |
| `c31f505` | **聊天落盘**（`data/chat_log/`，gitignore） |

其中四个原始问题（用户报的）：**用旧记忆回答当下** ✅ / **群里有话就接** ✅ /
**同话题回复同质** ✅ / **QQ 引用看不到** ✅

---

## 二、⭐ 这一轮真正的产出：十条认知

**比上面所有代码都值钱**，因为它们决定后面怎么做：

1. **唯一 assistant turn 通道最强** —— "她本人的话"比任何 system 说明都管用
   （同一事实走 voice_samples 3/5 被采用，走 corpus 0/5）
2. **注入的措辞，具体到什么粒度决定它会不会被落成词**
   （`'呃…'起头`→说"呃"；`表示收到但不接`→说"收到"；`不当回事地略过`→不渗）
3. **提示词约束不可靠，要代码兜底**（表情包"别再选最近发过的"，模型 2 次里 1 次不听）
4. **判断和生成写在一个调用里，模型会偏向生成**（主动发言"判不适合"判不出来就是这个）
5. **判据措辞稍动，结果从 0% 跳到 74%** —— 判定噪声大；要么概率采样，要么**严格偏向**
6. **三个功能的兜底方向必须分别设计**：接话门**放行**／表情包**不发**／主动发言**退回通知**
7. **合成场景自测 = 循环论证**（我造 8 个场景得 100%，真实数据一上就崩）
8. **关键词表扫描永远会漏**（漏了 `peer_5`，换个扫法才发现）
9. **裸模型 ≠ 走链路**（人格层接上后"幻觉式接话"自己就好了）
10. **难点从来不是"谁来跑"，是"判据是什么"** —— 判断该不该上 DSH 的关键

---

## 三、检索层：corpus 已修（asks 那条路的结论被推翻）

### corpus：阈值判据 → **LLM 语义判**（2026-09-25 第二轮，已做）

**原来的判断（上一轮）**：corpus 的问题在"库和 query 形态不同构"，
所以给每条经历挂"用户可能怎么说"当索引（`build_corpus_asks.py`，485 条）就能解决。

**这一轮实测：那个方案不成立** ——
- asks 当索引确实把正例从 0.44 拉到 **0.88**，**但噪声地板同步抬到 0.822**：
  `「明天几点开会」→「明天几点下课」0.822`（比部分正例还高）。
  语义、共享 bigram、IDF 加权重叠**三种都试过，全分不开**。
- 把 asks 拼进陈述一起编码（零代码改动的简化版）**也无效**：稀释后排名几乎不动。

**根因**：差别不在文本里，在**"她到底有没有这段经历"这个事实**——这是判断，不是相似度。

**真正的修法**（用户拍板"LLM 的判断是很有智慧的"）：**判据换成 LLM 判**。
- 门放行的照旧**直通**（高置信、零成本、**行为不变**）；门没放行的取 **top-N 候选**交 LLM 判。
- 因为实测发现：**正例对正确那条的排名是对的**（「你多高啊」→ 身高那条排第 1），
  只是分数（0.443）低于噪声地板——**排序有用、阈值没用**。
- **失败一律不带经历**（与接话门相反，与表情包一致）：宁可漏，不可错。
- 判据措辞是核心。起点「撞到同一个词/话题就算」→ **反例漏 3/10**（含评测用例「9点是你那边」，属退化）；
  改成「**用户是不是在问她**」→ **反例 0/10、正例 4/7**。
- 工具：`corpus_judge.py`；开关 `CORPUS_JUDGE=0`；评测 `retrieval_eval.py` **31/32**
  （新增 co6 你多高啊 / co7 明天几点开会 / co8 感冒吃什么药，**co7/co8 是只有 LLM 判才挡得住的反例**）。

⚠️ **评测脚本已改成镜像线上**（`corpus_items()`）：corpus 一路也走 LLM 判。
否则测的就不是线上真正跑的东西。

### 还没修的：phrase / preference 两路

| 路 | 噪声地板 | 真信号 | 现阈值 | 状态 |
|---|---|---|---|---|
| phrase | 0.575 | 0.573 | 0.40 | ❌ 中位数 0.461 都过阈值 |
| preference | 0.565 | 0.501/0.552/0.590 | 0.55 | ❌ 地板插在正例正中间 |
| voice_sample | 0.610 | — | ~~0.60~~ → **0.66** | ✅ 已修（eval 27→28/29） |
| core_story | 0.341 | — | 0.42 | ✅ |

**同一个病**（短文本余弦重叠），**同一个解法大概率也适用**——
corpus 这条走通了，phrase/preference 可以照抄（top-N + LLM 判）。
体检工具：`python scripts/threshold_scan.py`。

**`corpus_asks.json` / `build_corpus_asks.py` 先原样留着**（未入库）：
当索引不成立，但将来若"候选召回不够"可以用它扩候选。

---

## 四、下一步（按优先级）

1. **phrase / preference 两路照抄 corpus 的解法**（top-N + LLM 判）——
   同一个病（短文本余弦重叠）、同一个方子，corpus 已经走通。先用 `threshold_scan.py` 量地板。
2. **corpus 判据的实测观察**（刚上线，还没在真实对话里跑过）：看它**漏不漏**（宁可漏不可错，
   但漏太多就白修了）——真实问句里"她明明有的经历"召回率是多少，`data/chat_log/` 里能查。
3. **切片流水线**（用户要做的大工程）
   - 从**专门做切片的 B站 UP** 抓取，尽量自动化
   - ⚠️ 用户的关键判断：**切片里很多不是叙事、是"有意思的对话表达"** →
     **那些该进 voice_samples/behaviors，不是 corpus**
   - 即：**事件靠选择（切片→corpus），口癖靠统计（长转写→voice_samples）**
4. **素材库 + 工具链翻新**（大工程，见 `待办清单.md`）

---

## 五、其他还挂着的

- **corpus 不可复现**：322 条里只有 111 条能追溯到 `generate_statements`，
  「合并 → statement_final」**没有脚本**（手工的）。长度两极（75 条 >120 字，
  13 条 >300 字**会被 `MAX_ITEM_CHARS` 截断**）
- **`outputs/convert/` 那 877 条**：**不是"被遗忘的素材"**，是 `extract_persona`
  挖 behaviors 的原料（877 → 只出 2 条候选）。当 voice_samples 候选筛过一轮
  （最精标准 10% 通过率），**用户决定先放着**
- **记忆错位同类还剩几条**（用户决定先放着）：
  `behaviors #2`「以为连麦八点半就能准点结束」、`#8`「在抢票」、
  `voice_samples` 的 `peer_5`/`caught_8`
- **那句"无论怎么做都有点不好"的结论**：她的真实行为就是"给具体借口"，
  而具体事实一被复用就过期——**这是素材属性，改样本改不掉**，只能限制伤害
- **主动发言的两个偏差**：①"不适合主动说"判得不严 ②偏自言自语、带互动的少
- **时间驱动的主动发消息**（定时/自调度）——**没做**，那是说好要拿 DSH 试点的那个

---

## 六、五个开关（出问题可一键回退）

```
GROUP_GATE=0   群里退回"有话就接"
STICKER=0      不发表情包
PROACTIVE=0    动态/微博退回结构化通知模板
CHATLOG=0      不落盘聊天记录
CORPUS_JUDGE=0 corpus 退回"只靠关键词门放行"（判定的候选一条都不带）
```

⚠️ **改代码/数据后要重启 bot**（有进程内缓存）

---

## 七、DSH / agent 那条路（结论：暂不接）

调研过 `qq-bridge`（B站→DSH agent）。**结论：不换架构**。
理由：它解决的是"像个通用真人"（靠 32KB 规则 + 强基座），
我们解决的是"像某个具体的人"（靠真人素材检索）——**两条路**。
而且我们的检索管线/后处理/判据回归**在新栈上全要重建**。

**除非**要做"时间驱动的主动发消息"或"后台长任务"——那需要**自主调度**，是它的强项。
