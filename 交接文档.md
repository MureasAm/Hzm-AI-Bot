# 交接文档（下个会话先读这份）

> **给接手的会话**：这份是"当前状态 + 下一步"，比 CLAUDE.md 更细、比 待办清单.md 更即时。
> 读法建议：这份 → `CLAUDE.md`（黄金律） → `FILE_MAP.md`（改哪） → `待办清单.md`（还没做的）。
>
> 更新日期：2026-09-22 ｜ 测试基线：**431 个全绿**

---

## 一、最紧急：6 个提交卡在本地，**没推上去**

VPN 没开，直连与代理都不通。**先把这几个提交推了**（`git log origin/main..HEAD --oneline`）：

```
fa2ee8c  纯表情: 不让 LLM 猜含义 + 回退提示词里的行为规定
bb8980d  修微博"换了 cookie 还是不行"（旧 jar 顶掉新 cookie）+ 调研报告归档
5b157bb  修微博误报"该 UID 暂无微博"（ok=-100 掉进真值判断）
0a0f391  修语音"转出来了但没发出去"（插话取消打断在合成中间）
281d5a2  QQ 内置表情识别补全（QQ表情6 → 害羞）
6ac2676  terms 加 iceb + DEFAULT_MODEL 对齐 .env
```

推法：`git push origin main`（直连有时通），不行再 `-c http.proxy=http://127.0.0.1:7897`。
**注意代理端口会漂移，且要先确认 VPN 进程在跑**（`tasklist | grep -iE "clash|vpn|v2ray|xray"`）——如果进程都没有，换端口试也没用。

---

## 二、最高优先级未完任务：**纯表情的行为数据层**

### 背景（一个真实的坏例子）

```
19:29:24 用户：你今天也太冷脸家暴女了
19:29:35 她：家暴女？灰泽满这算家暴，那stories是没见过真家暴的   ← 嘴硬反击
19:29:43 用户：[可怜]                                       ← 被怼了，发个可怜求安慰
19:29:50 她：无语了？灰泽满又没说错什么                        ← 读成了"无语"
```

### 根因（两层，第二层是结构性的）

1. **probe 会"猜"表情含义**（LLM）。带上下文时会猜偏。
   → 已修：`core._qq_face_of()`，纯 QQ 表情**按字面还原**，不猜（含义就在名字里）。

2. ⚠️ **纯表情消息会跳过整个"检索 + 行为分类"块**：
   ```python
   if query_text and not is_emoji_msg(query_text):   # ← 纯表情进不来
       ...  select_behavior_item / classify_behavior ...
   ```
   **所以她拿不到任何【行为指令】与真人样本，数据层压根没参与**，只剩一段提示词在管。
   这是"为什么当时只能改提示词"的结构性原因。

### 待做（按顺序）

**① 加 behaviors 条目**（最高优先级，用户明确说下个文档做）
- 场景：**用户示弱 / 撒娇 / 求安慰时**（发 `[可怜]`/`[委屈]`、说"我只有你了""就不能喜欢我一点吗"这类）
- **必须带她的真实素材原话**（不是规则文字）——依据黄金律第 1 条：样本 > 规则
- 参考现有写法的格式：`name` / `trigger` / `response` / `samples[]` / `evidence[]`
- 现有 11 条里**最接近的是 #9「用户真诚感谢、关心你的情绪、深夜被惦记、触及孤独时」**，但那是"关心/孤独"，**不覆盖"示弱撒娇求安慰"** → 是空白

**② 让纯表情消息也走 L3 行为分类**
- 改动点：上面那个 `not is_emoji_msg(...)` 的条件——**跳过语义检索（corpus/样本/措辞），但不跳过 `select_behavior_item` / `classify_behavior`**
- 理由：行为归属是 L3（LLM 判意图 + 判别词），不是向量检索；纯表情恰恰最需要"情境 → 反应"的指引
- 做完后，`emoji_hint` 那段就真的只需要管"别读错情绪"

**③ 顺带检查**：`behavior_keywords.json` 的判别词要不要为这个场景补词（如"可怜""委屈""撒娇"）

### 已回退的东西（别再犯）

我在 `emoji_hint` 里写过 `委屈/可怜 → 这正是她该放软的时候：心软一下、别再嘴硬顶着`——**那是提示词补丁**，违反黄金律第 1 条；而且与同一会话里"删掉 😅 那行规定动作"的做法**自相矛盾**。已回退，现在只留"别读错情绪"的正确性约束。

---

## 三、用户澄清的需求：prior art 问题要的是"**可采纳的具体架构**"

**他明确纠正了理解**（重要）：

> "我的意思不是架构的重复，是有没有你检索到的产品或者作品**有实际使用、大众认可的更具体的架构**。我需要的**不是验证有没有错，我需要的是改进**。"

**所以下轮调研/检索的正确问法**：
- ❌ 不要问"我们的路线有没有前作"（那是求验证）
- ✅ 要问"**已经大规模在用、且被证明有效的具体机制是什么，我该怎么改进**"
- 要求：**具体到可以照着做**（参数、机制、取舍），**不要架构层面的泛泛对比**

已经拿到的、符合这个标准的两条（可直接采纳）：
1. **SillyTavern World Info 的 Cooldown / Sticky / Delay**（见下节）
2. **小冰的"多候选 + 学习式排序 + 阈值以上随机采样"**

---

## 四、用户第二个明确需求：**"拟人化的防重复"，不是机械防重复**

### 他为什么这么说

他做过两套机械防重复，**都出过问题**：
- `is_echo_reply`（整句级 LCS ≥60%）：还有效，保留
- `find_repeat_word`（防措辞固化）：**已删除**——实测 105 条回复触发 8 次，**其中 6 次拦的是她自己的自称"灰泽满"**，等于在纠正她的人设

**所以"检测到重复 → 强制重写"这条路，教训是：会误伤有意义的重复。**

### 拟人化的做法（结论）

**核心区别：让"重复"变成一个概率问题，而不是一个禁令。**

| | 机械式（❌ 别再走） | 拟人式（✅ 方向） |
|---|---|---|
| 时机 | **事后**：生成完 → 检测重复 → 强制重写 | **事前**：选材阶段就不选出重复的 |
| 判据 | **二值**：重复了就拦 | **概率**：用过的样本"降温"，自然选不到 |
| 对"有意义的重复" | 一律误伤（自称/口头禅/强调） | 不碰 |

**具体三条**：

1. **选材层加 Cooldown（来自 SillyTavern 事实标准）**
   给每条记忆/样本/经历条目挂"冷却窗口"：**激活后 N 轮内不能再被激活**。
   **关键细节（社区实现的精髓）：重复命中不刷新已生效的时长**——否则会被反复激活保活。
   > 人的类比：刚聊过这个话题，短时间内不会再提；而不是"禁止你说这个词"。

2. **生成层用"多候选 + 重排 + 随机选"（来自小冰）**
   同一 prompt 采 N 条候选 → 用轻量规则/判别打分（像不像她 + 与最近是否重）→ **从高分候选里随机选一条**。
   > **随机采样本身就是抗重复机制**——人说话本来就有分布、有随机性。重复是**概率低**，不是**被禁止**。
   > 最便宜版不需要训 ranker：N 条候选 + 几条规则打分即可。
   > 代价：N 倍推理成本。

3. **明确保留"有意义的重复"**
   自称"灰泽满"、口头禅、强调 —— **这些是人设，不是毛病**。任何防重复机制都要能放行它们（这正是删掉"防措辞固化"的教训）。

---

## 五、已完成/已归档（本轮会话产出）

### 代码改动（都在上面的待推提交里）
- **换行归一**（`clean_reply`）：断行跟在标点后→接上，跟在实词后→补逗号
- **记忆时间戳**：长期注入 `last_seen` 间隔；短期每轮存时间戳，生成前注入一行"距上一轮多久"（老数据兼容）
- **记忆回流修复**：`append_user_history` 改存 `clean_reply(reply)`
- **罗马音修复**：清掉 terms/behaviors 注入文本里的罗马音，保留在 `aliases`
- **期数词表**：前辈(20-26期)/后辈(28-29期)，`<期数>+7` 规律，用带数字边界的 pattern
- **删掉"防措辞固化"**（理由见上）
- **【一致性规则】改成"只管事实"**：旧文案"借口要与之前保持一致"是直接教复读
- **事实类通道加时效**：schedule 近况 TTL(7天)、群事件/最近的记忆带相对时间、新增按天粒度的 `format_day_gap()`
- **LLM 响应解析加护栏**：`config.extract_chat_content()`，9 个调用点统一；`deepseek-flash` 默认开思考会吃光 max_tokens
- **兜底文案不进记忆**：`is_fallback_reply()`
- **QQ 表情表**：`qq_faces.py`（248 条，从 QQ 客户端配置提取）
- **微博三修**：ok=-100 真值判断坑 / 空列表误报 / **旧 jar 顶掉新 cookie**（存 `_base_fp` 指纹失效）
- **语音**：`asyncio.shield` 防插话取消打断合成；`file://` 用正斜杠

### 文档
- `调研-角色对话AI架构.md` —— 三轮深度调研（含**证据强度分级**，别只看结论）
- `待办清单.md` —— 已判定"不是问题"的登记（防顺手改回去）

---

## 六、其他发现（待定，别急着动）

### 1. "stories" 挂在句尾 —— **用户已确认是长期记忆导致，且他认可**
来源：`memory_manager.py:273`
```python
parts.append(f"这个绿冻叫{name}，聊天时用TA的名字称呼TA，别老叫TA'这个绿冻'。")
```
用户的昵称是 `stories`（uid 3076669330，137 次互动）。那句 **"聊天时用TA的名字称呼TA"** 让她往句尾挂名字。
**用户判断"在语境下没有问题"** → 暂不动。若要改，只需把这句的措辞从"用TA的名字称呼TA"改成"知道TA叫什么就行"。

### 2. corpus 要不要加日期 —— **已判定：不建议**
- corpus 322 条**没有任何日期字段，而且是故意的**（蒸馏铁律第 1 条：严禁时间戳，时间要转化为自然语境）
- **注入时已经告诉模型"都是曾经发生的，不是现在"**
- 用户观察到的是"旧记忆被检索出来" → 那是**检索层**问题（纯相关性），**加日期解决不了**
- 且精度只到"某个月" → **假精度比没有更糟**

### 3. RRF 要不要换 —— **结论：加一层，不换掉**
- RRF 出身是**搜索结果融合**，优化"哪条最相关"；而"复刻说话方式"要的是"**这条是不是她的、她在这个情境下会不会这么说**"——相关性不是第一判据
- 但 RRF 的价值（合并异质列表）仍成立 → **不必换算法**
- 该加的优先级：**① 防重复（Cooldown）> ② 情境匹配(L3 已在做) > ③ 真伪(已做到) > ④ 时效(部分做了)**

---

## 七、调研的两个"别做"（省人力）

- ❌ **别调 RRF 的 `k`** —— TOIS 2023：调参不跨域迁移
- ❌ **别为了"更像她"换开源模型做微调** —— LREC 2026 唯一头对头：SFT 真实性垫底(5.81，低于全部提示基线)；且微调副作用（多样性下降 distinct-2 0.625→0.565）**正撞"机械感"痛点**。证据强度：弱（单一论文/日语/8B/无人类评测）

---

## 八、未解问题（调研没答上、值得再做）

1. **"措辞像" vs "性格像"是两个维度**，现有评测（大五人格 + LLM 判定）**最容易放过"措辞不像"**（PersonaGym 作者自曝：自动 4.5/4.5/4.0，人类 2.0/2.0/3.0）→ **该补一条专门测口癖/措辞的判据**
2. 没有任何主流基准做过"真人语料检索 few-shot vs 微调"的对比（PersonaGym 全文 `few-shot` 出现 0 次）
3. importance 的锚点如何为"角色一致性"重写并校准（GA 的锚点是"人生大事"，这里需要"最能体现她人设"）
