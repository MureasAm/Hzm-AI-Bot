# 调研报告：角色一致性对话 AI 的底层架构

> **这份文档回答什么**：为了让"一个 AI 像某个具体真人那样说话"，业界有哪些经过外部验证的方法？本项目的架构（六路语义检索 + RRF + 十层注入）处在什么位置？该往哪走？
>
> **怎么来的**：三轮自动化深度调研（每轮 100+ 个子代理：拆检索角度 → 并行搜索 → 抓取去重 → 抽取可证伪论断 → **每条 3 票对抗验证**，2/3 反驳才淘汰）。三轮共抽出 115+ 条论断，验证后存活 21+ 条。
>
> **生成日期**：2026-09-18 ｜ **被调研系统**：本项目（灰泽满 QQ bot）

---

## 〇、一句话结论

1. **"旧记忆被翻出来"不是 bug，是"纯相关性检索"的固有失效模式**——文献、开原生态、商业产品三方都印证。
2. **"换开源模型做微调会让她更像她"——文献不支持**，而且微调的主要副作用（多样性下降）正好是本项目最烦的"机械感"。
3. **本项目在多个设计上独立重造了社区事实标准**（关键词触发、token 预算、插入深度），但**缺三个成熟机制**：记忆条目的**冷却窗口**、记忆层的**预算闸门**、一条**确定性召回路径**。
4. **本项目缺的那块评测**：现有"大五人格 + LLM 判定"最容易放过"措辞不像"——而这恰是"像她"的核心。

---

## 一、第一篇：检索与记忆打分（学术）

### 1.1 "旧记忆被翻出来"的机制根因

本项目的 `rrf_fuse`（`retrieval.py:384-397`）是 `fusion_score = weight / (k + rank)`——**只反映名次，不反映时效与重要性**。

一份 ICLR 2025 的公开复现工作给出的批评，几乎就是本项目的症状：

> **Relevance only → ignores that yesterday's discussion matters more than last year's.**
> （只看相关性 → 会忽略"昨天聊的比去年的更重要"）

### 1.2 Generative Agents 的三因子——但**不能照抄**

斯坦福 Park et al.（UIST 2023，同行评审）的记忆检索打分：

```
score = α·recency + α·importance + α·relevance     ← 三项各自 min-max 归一到 [0,1] 再相加
recency    = 0.995 ^ 距上次被检索的小时数
importance = 创建时 LLM 一次性打 1–10 分（1=刷牙，10=分手），之后不再更新
```

**⚠️ 对本项目最关键的发现：照抄它会加剧问题，不是缓解。**

GA 的 recency 锚在 `last_accessed`——**被检索一次就刷新时钟**，形成正反馈：

```
被检索 → 刷新 → 更容易再被检索 → 计时器再刷新 → ...
```

**一条被反复捞出的旧记忆会持续保持高温。** 而本项目要治的正是"旧事重提"。

**而且官方代码的实际权重是 `gw=[0.5, 3, 2]`**——relevance 是 recency 的 6 倍。**作者自己的调参结论是"相关性主导"**，削弱了"加时效必然有正收益"的暗示。

### 1.3 GA 的外部验证强度——**必须收窄**

| 项 | 事实 |
|---|---|
| ✅ 有 | UIST '23 受控实验，**100 名被试、IRB 批准**、within-subjects、believability 排序；完整架构 μ=29.89 最高，逐级消融单调下降，Cohen's d=8.16, p<0.001 |
| ❌ 但 | 消融的粒度是**"记忆流入口类型的有无"**（observation/reflection/planning），**从未单独消融三个打分数项，也从未扫 α** |
| ❌ 而且 | 论文自己的 Future Work 把"微调 relevance/recency/importance 函数"列为**未完成** |
| ❌ 并且 | **未找到独立第三方复现** μ=29.89；作者自承众包基线"不代表人类上限" |

**→ 结论：「给检索加时效/重要性维度」这个方向可信（它是机制描述），但"加了就更好"在这篇论文里属于外推，本项目要自己 A/B 才算数。**

### 1.4 RRF 的适用边界（TOIS 期刊，直接命中融合层）

Bruch, Gai & Ingber《An Analysis of Fusion Functions for Hybrid Retrieval》（**ACM TOIS 42(1), 2023**，被引 100+）：

| 结论 | 对本项目的含义 |
|---|---|
| **RRF 只依赖名次、主动丢弃分数的幅值** | 时效/重要性这类**幅度信号在设计上就进不来**，不是调参能解决的 |
| **k=60 是经验值**（Cormack SIGIR'09 原文："fixed during a pilot investigation"），来自 TREC 文档检索试点 | 它不是理论最优值 |
| **RRF 对参数敏感，且域内调好的参数不跨域迁移** | ⚠️ **别花人力调 k** |
| **归一化分数的凸组合（TM2C2, α=0.8）在 9 个数据集上显著优于 RRF**（paired two-tailed t-test, p<0.01） | "融合层要换"的候选 |

**边界**：该论文研究的是 BM25+dense **两路文档检索**融合；本项目是**六路异质列表做对话上下文选择**——外推属推断，论文未验证该场景。

### 1.5 记忆粒度与时效索引（LongMemEval, ICLR 2025，数据与代码公开）

| 发现 | 对本项目的含义 |
|---|---|
| **更细的轮次（round）优于摘要/事实压缩**（GPT-4o 作 reader）；压缩"因信息损失降低 QA 准确率"，唯一例外是多会话聚合题 | 本项目的 322 条直播陈述正是**"事实压缩"形态**，单用会伤细节题 → **不是替换，而是并存一层更细粒度片段** |
| **显式时间索引 + LLM 时间范围查询扩展** → 时序题 recall **+11.3%（轮次）/ +6.8%（会话）** | 可落地，**但每轮多一次 LLM 调用** |
| ⚠️ **必须用强模型**：Llama 8B 在问题**本无**时间指代时会**幻觉出时间范围、错误剪枝**，反而更差（Recall@5 0.421 → 0.384）；GPT-4o 能正确拒答 | 本项目用 DeepSeek 可行 |

### 1.6 反方证据——**本项目路线未被否定**

有一篇 position paper（arXiv:2604.27707）宣称"检索是查找不是记忆""存在泛化天花板"。**但证据弱**：

- **未经同行评审**、单一团队、无独立复现
- **定理是条件性的、作者自陈可证伪**：挂在 `ᾱ<1` 假设下，原文"**When ⊕ is broadly general, ᾱ→1 and the separation vanishes**"
- **v2 主动退缩**：v1 称"再多的上下文与检索质量都无法克服"（ceiling 出现 11 次），v2 改为在显式定义域内限定（ceiling 出现 0 次）
- **它自己承认**：「These mechanisms can support sophisticated inference and are often **preferable for scoped, changing, inspectable, or deletable information**」——**角色人设与可删记忆正落在这个区间**

---

## 二、第二篇：已上线产品的工程实践

### 2.1 开源事实标准：SillyTavern + 角色卡 V2/V3 + World Info/Lorebook

**机制在官方文档与发版源码里逐行可查、可复现**。

| 维度 | 本项目 | SillyTavern（社区事实标准） |
|---|---|---|
| 触发 | **六路全语义检索** | **关键词纯子串匹配**（可正则） |
| 向量 | 核心 | **官方降级为"可选插件"**，明说"不可预测、无法保证插入哪些条目"，建议想要确定性的人别用 |

**这是主动的设计取舍**：牺牲语义泛化，换**可预测、可调试**。

**本项目缺的三个机制**：

1. **硬 token 预算**：默认占上下文 **25%**（`world_info_budget`）；**预算耗尽后即使命中关键词也不再激活任何条目**。
   （本项目是 `continue` 跳过、继续试下一条，语义更宽松）
2. **显式优先级 + 插入深度**：`Order` 数值越大 → 插得越靠后 → **对输出影响越大**；还能指定"距对话末尾第几条"。**把"该塞哪一层"做成了可调参数。**
3. **定时效果（Timed Effects）** ← **直接对症"翻旧账"**：
   - `Cooldown`：激活后 N 条消息内不能再激活
   - `Sticky`：激活后保持 N 条
   - `Delay`：至少 N 条后才允许激活
   - **关键细节：重复命中关键词不刷新已生效的时长**——正是"同一条记忆不要被反复捞起"要的语义

> 两个精度提示：官档说"常驻条目先插入"，但 release 1.19.0 源码里找不到该排序分支（降级为"文档声称"）；角色卡规范里的 `token_budget`/`priority` 字段 **SillyTavern 根本不读**（`convertCharacterBook` 只映射 keys/content/enabled/insertion_order/position）。

### 2.2 商业产品：结构化事实库 + 有界记忆

| 产品 | 做法 | 证据强度 |
|---|---|---|
| **Replika** | 记忆是**用户可见、可逐条删除、可手动添加**的条目表；写入门槛低（闲聊自然提及即记）；**对话记录 4 个月过期，提取出的记忆不过期** | ⚠️ 弱——全是厂商 help center 自述，无第三方验证 |
| **Character.AI** | **公开承认记忆有硬容量上限**，用满即遗忘/压缩，并把"剩余记忆量"做成**用户可见仪表** | ⚠️ 中——官方博客在本机被 DNS 污染不可直连，仅二手交叉确认 |

**→ 商业产品没有"无限记忆"，都是有上限 + 淘汰 + 用户可看见可纠正。本项目现在是零上限、零淘汰、用户不可见。**

### 2.3 小冰（唯一有公开发表工程论文的大规模产品）

| 维度 | 做法 |
|---|---|
| 跨会话记忆 | **没有**。会话状态每次清空，只存本场；跨会话人设/画像走**另一套预编译 profile 库** |
| 人设怎么进模型 | **不是"提示词写一段描述"**，而是编码成 **key-value 向量注入解码器隐层**——"角色 = 模型的输入特征"，不是"上下文里的文本" |
| 怎么出回复 | **多来源候选池 → 学习式排序（boosted tree）→ 阈值以上随机采样**。**随机采样本身即抗复读机制** |
| 怎么防"聊死" | 专门分类器实时检测**"对话将死"**：①回复只是重复用户输入/不含新信息 ②用户变敷衍（"OK"/"I see"）③本轮走了兜底文案 → **主动切话题**（1 个月 A/B 让 CPS 提升 0.5） |
| 人工评测 | 混合系统均分 1.09 > 纯检索 0.87 > 纯神经生成 0.40 |

> ⚠️ **时点限定**：以上来自 **2018-2019 年的架构论文**，描述**LLM 之前**的架构。小冰 2020 年独立成公司后转 LLM 路线，**现行实现无公开证据**。
> ⚠️ 表 3 的混合系统**只合并了两路**（神经生成 + 配对库检索），1.09 不能读成"三路混合得分"。

### 2.4 反方：别用记忆类 benchmark 榜单挑方案

某审计仓库（单人、非同行评审、对被测方持对抗立场，但**原始逐题数据可复现**）发现：

- 评测用的 LLM judge **极度宽松**：**故意写错但"话题沾边"的答案接受率 62.81%**（单跳 68.53%），是"具体但错误"策略（10.61%）的**约 6 倍**
- 在 LoCoMo 上，**用相同 CoT 提示但完全不用记忆系统**（整段对话全塞进上下文，约 21.6k token）拿到 **92.62%**，反超某记忆系统公布的 92.32% 及其自称的 full-context 基线 91.21%
- **增益主要来自提示词写法（同模型同上下文下 prompt 单独造成 +10.67 分）与答案更长**

**→ 选型护栏：别用榜单挑记忆中间件；要自建"以本项目关心的失败模式为判据"的小评测（旧记忆误召率、措辞像不像、复读率），并用严格 judge（不要"沾边就算对"）。**

---

## 三、第三篇：人格保真——"微调 vs 检索样本"

### 3.1 唯一的头对头对比（LREC 2026, NTT）——结果反直觉

| 方法 | 角色真实性（10 分制，单轮 LLM 判官） |
|---|---|
| **DPO**（偏好对齐微调） | **7.96** ← 最高 |
| **检索 few-shot**（本项目做法） | 6.55 |
| **SFT**（常规微调） | **5.81** ← **低于全部三个提示基线** |

多轮 GPT-4o 评判方向一致（SFT 6.2 最低，DPO+CK 9.6 最高）。

**→ "微调了就一定更像"不成立；方法选择比"是否微调"更关键。**

> ⚠️ **证据很弱**：单一论文、单一团队、**仅日语**、基座 Llama-3-ELYZA-JP-8B、**无人类评测**、无显著性检验；作者自认未测 70B 级以上模型。**不能直接外推到闭源 DeepSeek。**

### 3.2 "措辞像"与"性格像"是两个可分离维度（三个独立团队一致）

| 方法 | 措辞复现（BERT-F1） | 角色真实性 |
|---|---|---|
| SFT | **0.690（最强）** | 5.81（最差） |
| 检索 few-shot | 0.675 | 6.55 |
| DPO | **0.625（最差，低于零样本 0.643）** | **7.96（最强）** |

旁证：CharacterEval 中 **GPT-4 在"人格回测"排第 1（0.694），但在中文 Human-Likeness 上排 14/15（3.143）**。

**→ 评测与优化必须把两者拆成两条独立指标，不要用一个"像不像"总分判定。**

### 3.3 微调的副作用——**正好撞本项目的痛点**

| 副作用 | 数据 |
|---|---|
| **多样性下降** | distinct-2：真人 **0.714** > few-shot **0.625** > SFT **0.565** > DPO **0.469** |
| **输出变短变简单** | SFT 输出长度 16.7 token，全表最短 |
| **靠降 temperature 补救 → 引发复读** | 实验者自述 |
| **"换角色但语气不变"** | 专为角色微调的 CharacterLLM 在"辨识度"上仅 **38.98**（真人 72.91），**全表最低**——归因于**预训练模型的固有腔调抹不掉** |

**→ 微调最大的风险就是"变得更机械"——正是本项目最烦的问题。**

### 3.4 本项目缺的那条评测：LLM 判官最容易放过"措辞不像"

| 证据 | 内容 |
|---|---|
| PersonaGym 作者自曝反例 | 一个 22 岁伦敦作家角色：**自动评分 4.5/4.5/4.0，人类 2.0/2.0/3.0** |
| CharacterEval 判官相关性 | 流畅/连贯 0.475~0.604（还行）；**Persona Behavior 0.244~0.305**；**Knowledge Accuracy 0.182~0.239**（很差） |
| LLM 成对偏好 | 对**人类写的**文本相关系数 rp=0.632，对 **LLM 自己写的** rp=-0.001；**加 6 个 few-shot 示例可提到 0.659** |

**→ 现有的"大五人格开放题 + LLM 判定"能测"性格像不像"，但测不出"措辞像不像"。得单独加一条针对口癖/措辞的判据。**

### 3.5 一条**反向支持本项目**的证据

InCharacter 消融：加入 **3 条通用**（未按角色定制）的 in-context 示例，**人格对齐度反而下降**（BFI 63.3 → 56.1）。作者原话：

> "the few-shot examples are not customized for individual characters. Instead, incorporating responses from other personas in the examples might interfere with the intended personas"

**→ 示例必须来自该角色本人。本项目检索真人语料的做法，方向正确。**

---

## 四、合并行动清单

| 优先级 | 做什么 | 依据 | 代价 | 对症 |
|---|---|---|---|---|
| **1** | **给记忆/经历条目挂 Cooldown**（激活后 N 轮内不能再激活；**重复命中不刷新**） | SillyTavern 事实标准 | 小。需把状态提到跨会话（社区实现绑定单次 chat） | 旧记忆翻出来 |
| **2** | **给记忆层加 token 级预算 + 淘汰**（现在零闸门） | Character.AI + ST | 小 | 旧记忆泛滥 |
| **3** | **补一条确定性（关键词）召回路径**，与语义分工 | 社区事实标准 | 中 | 可调试性 + 兜底 |
| **4** | **加一条专门测"措辞像不像"的评测** | 学术：LLM 判官最漏这个 | 小 | 评测盲区 |
| 5 | 多候选 + 重排 + 达标后随机选 | 小冰 | 中（N 倍推理成本）；可先做最便宜版：同 prompt 采 N 条，轻量规则打分选一条 | 机械感 |
| 6 | "对话将死"检测 → 主动切话题 | 小冰 | 中（别切太勤，会打断正常冷场） | 机械感 |
| ❌ | **换开源模型做微调** | 文献不支持；副作用撞痛点 | —— | —— |
| ❌ | **调 RRF 的 k** | TOIS：调参不跨域迁移 | —— | —— |
| ❌ | **推高 repetition penalty 防复读** | 社区共识：不可靠且伤语法 | —— | —— |
| ❌ | **用记忆类 benchmark 榜单挑方案** | 审计：judge 宽松、增益多来自提示词 | —— | —— |

---

## 五、证据强度对照

| 结论 | 来源类型 | 强度 |
|---|---|---|
| SillyTavern / 角色卡规范机制 | 官方文档 + 发版源码逐行可查 | **强** |
| RRF 的边界（TOIS 2023） | 同行评审期刊，被引 100+ | **强**（但场景为文档检索，外推属推断） |
| LongMemEval（粒度/时效） | ICLR 2025，数据与复现代码公开 | **强** |
| GA 三因子机制 | UIST 2023 同行评审 + 官方代码 | **强**（机制描述） |
| GA 的"外部验证" | 作者团队自跑 100 人实验 | **中**（消融粒度不对口；无第三方复现） |
| 微调 vs 检索的头对头 | LREC 2026 单一论文 | **弱**（日语/8B/无人类评测/无显著性检验） |
| 微调副作用 | ACL 2024 + LREC 2026 + 日本言语处理学会 2025 | **中—强**（多团队一致） |
| 小冰架构 | 同行评审论文，但 **2018-2019 年**、LLM 之前 | **中**（现行实现无公开证据） |
| Replika | 厂商 help center 自述 | **弱**（无第三方验证，且与其隐私政策矛盾） |
| Character.AI | 官方博客，本机 DNS 不可直连 | **中**（二手交叉确认） |
| benchmark 审计 | 单人、非同行评审、对抗立场 | **弱**（但数字可由原始数据复现） |
| 反方 position paper | arXiv，未经评审，v2 自退 | **弱**（不构成"路线到顶"的证据） |

**时间敏感性**：SillyTavern 结论对应 `release 1.19.0` / 2026 年线上文档；**Character.AI 记忆系统 2026-04~05 刚重建**，产品行为变化快。学术侧评测对象多为 2023-2025 初的模型代际，2025 新模型已在"语言习惯"维度上越过 4 分线。

---

## 六、未解问题（诚实标注）

1. **没有任何主流基准做过"真人语料检索 few-shot vs 微调"的正面对比。** PersonaGym 全文 `fine-tune` 出现 2 次（且在相关工作段落）、`few-shot` **0 次**；InCharacter 只测提示条件化。**对本项目"中文 + 真人语料"这个约束，文献是零直接证据。**
2. **importance 的锚点该怎么为"角色一致性"重写？** GA 的锚点是"人生大事"，本项目需要"最能体现她人设/最不该忘的设定"——中文直播语料上如何写 few-shot、如何跨批校准，无外部证据。已知隐患：通用 importance 提示词分布向中段聚集，区分度可能不足。
3. **recency 该锚在"创建时间"还是"上次访问"？** GA 原设计会形成 rehearsal 正反馈。本项目应引入创建时间轴的有效性判定（created_at / valid_at-invalid_at）还是"距上次主动提及"惩罚项？无被验证过的对话人格场景实证。
4. **rank 融合 vs key 合并**：对"异质人格资产路由"（corpus/措辞/行为/偏好/核心记忆），哪条更优？唯一的旁证（LongMemEval 的 rank merging 劣于 key merging）**已被 2-1 否决**，问题本身未被回答。
5. **自动评测与人类判断的相关性有多大？** 方向（消融 A/B + 人类排序）明确，但无样本量/统计功效下限的可操作指导。

---

## 七、主要来源

**学术**
- Park et al. *Generative Agents* (UIST 2023) — https://dlnext.acm.org/doi/abs/10.1145/3586183.3606763
- Bruch, Gai & Ingber. *An Analysis of Fusion Functions for Hybrid Retrieval* (ACM TOIS 2023) — https://ar5iv.labs.arxiv.org/html/2210.11934
- Wu et al. *LongMemEval* (ICLR 2025) — https://github.com/xiaowu0162/longmemeval
- Cormack et al. *Reciprocal Rank Fusion* (SIGIR 2009) — https://www.semanticscholar.org/paper/Reciprocal-rank-fusion-outperforms-condorcet-and-Cormack-Clarke/9e698010f9d8fa374e7f49f776af301dd200c548
- Wang et al. *InCharacter* (ACL 2024) — https://aclanthology.org/2024.acl-long.102/
- *PersonaGym* (EMNLP Findings 2025) — https://aclanthology.org/2025.findings-emnlp.368/
- Otsuka et al. (LREC 2026) — https://aclanthology.org/anthology-files/pdf/lrec/2026.lrec-main.209.pdf
- *CharacterEval* (ACL 2024) — https://arxiv.org/abs/2401.01275
- Abdulhai et al. (NeurIPS 2025) — https://mlanthology.org/neurips/2025/abdulhai2025neurips-consistently/

**工程 / 产品**
- SillyTavern World Info 文档 — https://docs.sillytavern.app/usage/core-concepts/worldinfo/
- Character Card V2 规范 — https://github.com/malfoyslastname/character-card-spec-v2/blob/main/spec_v2.md
- SillyTavern 源码 `world-info.js` — https://raw.githubusercontent.com/SillyTavern/SillyTavern/release/public/scripts/world-info.js
- Replika Help Center（Memory / Diary）
- Character.AI Blog（Memory）
- LoCoMo 审计仓库 — https://github.com/dial481/locomo-audit

---

## 八、附：本轮调研的安全告警（留档）

第二轮（产品篇）的 106 个子代理中，**有 1 个触发了安全策略告警**：

> Chrome-MCP 导航到了外部域名 `example.com`——该域名与本次研究无关，且不在任何已批准白名单内。

**处置**：研究子代理仅持有联网工具、作用域内无本地数据；`example.com` 是 IANA 保留测试域名，判断为**代理在试探浏览器工具**而非数据外传。**但无法完全验证**（只能看到结果、看不到全部动作）。
与其关联的结论（**"推高 repetition penalty 防复读不可靠"**）是长期存在的公开技术共识，故保留，**但在此标记存档**。

---

## 九、本轮调研**纠正**的两处先前判断

1. **我说"给 RRF 加 recency、锚在 last_accessed"** → **错**。那会形成正反馈、**加剧**"旧事重提"。正解是**创建时间轴上的有效性判定**。
2. **我说"检索结果无上限塞入"** → **错**。本项目检索路**已有字符预算闸门**（`truncate_by_budget`，TOPK=6 / 1200 字符 / 单条 300 字符）。
   **真正的缺口**：全仓**无 token 级计数器**（`grep tiktoken/count_tokens` 零命中）；预算单位是**字符**且**只覆盖检索路**；`build_memory_context`（`memory_manager.py:257`）由记忆卡字段**直接拼装、无总量闸门**。
   **→ "旧记忆泛滥"若归因，应指向"未被预算约束的记忆层注入"。**
